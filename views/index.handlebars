{{!-- for each statements goes here --}}
<h1>The burger Resturant</h1>



{{!-- Logic goes here --}}

<h2>The most flavorful burgers awaits!</h2>


{{!-- Logic goes here --}}

<ul>
  {{#each burger}}
    {{#unless hungry}}
      {{> burgers/burger-block hungry=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
  {{#each burgers}}
    {{#if hungry}}
      {{> burgers/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>



{{!-- Add a burger form --}}


<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Hungry?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Hungry?<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form>


{{!-- Button to add burger goes in this file also --}}